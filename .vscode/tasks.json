{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Update requirements",
            "type": "shell",
            "command": "pip-compile --upgrade --extra=shell --output-file=requirements_all.txt setup.cfg && pip-compile --upgrade --extra=dev --output-file=requirements_test.txt --pip-args='-c requirements_all.txt' setup.cfg",
            "problemMatcher": []
        }
    ]
}
